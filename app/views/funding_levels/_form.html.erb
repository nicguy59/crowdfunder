<%= form_for([@project, @funding_level]) do |f| %>

  <%= f.label :reward_name %>
  <br>
  <%= f.text_field :reward_name, required: true %>
  <br>
  <%= f.number_field :amount, default: 0 %>

<% end %>