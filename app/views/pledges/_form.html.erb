<%= form_for([@project, @pledge]) do |f| %>

  <%= f.label :name %>
  <br>
  <%= f.text_field :name, required: true %>
  <br>
  <br>
  <%= f.label :email %>
  <br>
  <%= f.text_field :email, required: true %>
  <br>

  <%= f.label :amount, 'Enter a specific amount:' %>
  <br>
  <br>
  <%= submit 

<% end %>